import "./Navbar.css";

export default function Navbar() {
  return (
    <header className="navbar">
      <div className="container">
        <h2>Dashboard</h2>

        <button>Logout</button>
      </div>
    </header>
  );
}